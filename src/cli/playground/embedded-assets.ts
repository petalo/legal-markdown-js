// AUTO-GENERATED during binary builds by scripts/embed-web-assets.ts
// This stub (empty map) is what lives in git.
// The binary build overwrites this file, then restores it via `git checkout`.
export type AssetEntry = { content: string; mime: string };
export type AssetMap = Record<string, AssetEntry>;
export const EMBEDDED_WEB_ASSETS: AssetMap = {};
